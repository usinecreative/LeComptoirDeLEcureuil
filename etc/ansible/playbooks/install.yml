---
- hosts: all
  become: no

  tasks:
     - include_tasks: ../tasks/install/database.yml
     - include_tasks: ../tasks/install/filesystem.yml
     - include_tasks: ../tasks/install/apache.yml
     - include_tasks: ../tasks/install/github.yml

  handlers:
    - name: Restart apache
      become: yes
      service:
        name: apache2
        state: restarted
        enabled: yes
