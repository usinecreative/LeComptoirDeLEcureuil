server '{{ server_name }}',
user: '{{ user }}',
roles: %w{web app db},
ssh_options: {
    user: '{{ user }}',
    keys: %w({{ ansible_ssh_private_key_file }}),
    forward_agent: yes,
    auth_methods: %w(publickey password)
}
