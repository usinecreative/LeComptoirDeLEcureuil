{% extends "::layout.html.twig" %}

{% block title %}{{ page.title }}{% endblock %}

{% block content %}
    <div class="col-lg-9 main-content free-page">
        <div class="page-content">
            <h1>{{ page.title }}</h1>

            {{ page.content | raw }}
        </div>
    </div>
{% endblock %}
